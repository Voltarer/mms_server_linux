# MMS Server 

Минимальный проект MMS-сервера (IEC 61850) с интеграцией аппаратных
портов коммутатора Realtek.

---

## Структура проекта
* **`src/`** — Исходный код сервера.
    * `main.c` — Основная логика: опрос портов и обновление модели.
    * `model.icd` — Описание логической модели устройства (SCL).
    * `static_model.c/h` — Файлы, генерируемые из ICD для работы сервера.
* **`lib/`** — Библиотеки и SDK.
    * `libiec61850/` — Стек протоколов МЭК 61850.
    * `sdk/` — Заголовочные файлы и библиотеки Realtek.
* **`build/`** — Папка с готовым бинарным файлом.

---

## 🛠 Скрипты управления

| Скрипт | Назначение |
| :--- | :--- |
| `./gen.sh` | **Генератор модели.** Превращает XML (`.icd`) в Си-код. Запускать при изменении состава портов или сигналов. |
| `./build.sh` | **Компилятор.** Собирает проект под архитектуру MIPS. |
| `./setup.sh` | **Подготовка к сборке.** Скачивает и собирает библиотеку libiec61850 |


## Быстрый старт

### 1. Установка библиотеки libiec61850

``` bash
chmod +x setup.sh
./setup.sh
```

### 2. Подготовка

-   Скачайте и поместите компилятор в `../toolchain/`
-   Переместите папку sdk в `lib/`, должно получиться `lib/sdk`

### 3. Сборка под MIPS

``` bash
chmod +x build.sh
./build.sh
```

Готовый бинарный файл:

    build/mms_server_mips
## 🛠 Необходимые инструменты

Для сборки проекта вам потребуются дополнительные файлы, которые не включены в основной репозиторий из-за их размера. 

**Скачайте их со страницы [Releases](../../releases/latest):**

1. **Компилятор**: `mips-buildroot-linux-uclibc.tar.xz` (распаковать в `../toolchain/`)
2. **SDK Headers**: `sdk.tar.gz` (распаковать в `lib/`)

