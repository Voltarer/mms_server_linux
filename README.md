# MMS Server 

Минимальный проект MMS-сервера (IEC 61850) с интеграцией аппаратных
портов коммутатора Realtek.

![Version](https://img.shields.io/badge/version-2.3.6-blue)

## История изменений

<details>
  <summary><b>Посмотреть историю версий (Changelog)</b></summary>

  ### v2.3.6
  * Добавлена функция изменения MTU
  * Добавлена функция изменения Flow control
  * Добавлена функция изменения Авто согласования
  * Вывод данных : какая скорость, режимы работы ,MAC-адрес, имя и номер порта.
  * Переработана модель данных

  ### v2.3.5
  * Добавлена функция изменения скорости
  * Переработана модель данных

  ### v2.3.4
  * Вырезана сборка винды под винду
  * Переработана модель данных
  * Теперь работает функция включение и выключение физического порта

  ### v2.3.2
  * Функции информации по порту теперь в библиотеке (собирается при сборке финального бинарника - MIPS)
  * Теперь статичный линковщик

  ### v2.3.1
  * Обновлена сборка под MIPS ( добавлен динамический линковщик)

  ### v2.3.0
  * Добавлен netlink
  * Обновлены автоматические скрипты для сборки и установки под разные ОС
  * Обновлена сборка под MIPS 

  ### v2.2.1
  * Добавлены новые атрибуты в LCCH, LPCP, LPHD

  ### v2.2.0
  * Добавлен общий скрипт для сборки и установки под разные ОС

  ### v2.1.2
  * Исправил модель: добавлен полноценно LPHD,убрал лишние (GGIO, phyHealth), добавил новые атрибуты из документа

  ### v2.1.1
  * Настроил связи в LCCH: для каждого из 28 портов добавил ссылки PortRef и RedPortRef, указывающие на соответствующие узлы Bridge/LPCP#.
  * Пронумеровал порты в LPCP: в каждом узле LPCP (с 1 по 28) прописал его порядковый номер в параметре PortNum.setVal.
  * Привязал все пути к логическому устройству Bridge, как того требует текущая конфигурация модели.
  * Подготовил XML-шаблоны: определил точный формат блоков <DOI> и <DAI> для корректной работы генератора кода.

  ### v1.0.0
  * Первый стабильный релиз.
</details>

---

## Структура проекта
* **`src/`** — Исходный код сервера.
    * `main.c` — Основная логика: опрос портов и обновление модели.
    * `model.icd` — Описание логической модели устройства (SCL).
    * `static_model.c/h` — Файлы, генерируемые из ICD для работы сервера.
* **`lib/`** — Библиотеки и SDK.
    * `libiec61850/` — Стек протоколов МЭК 61850.
    * `realtek/` — Заголовочные файлы и библиотеки Realtek.
* **`build/`** — Папка с готовым бинарным файлом.

---

## 🛠 Скрипты управления

| Скрипт | Назначение |
| :--- | :--- |
| `./gen.sh` | **Генератор модели.** Превращает XML (`.icd`) в Си-код. Запускать при изменении состава портов или сигналов. |
| `./install.sh` | **Генератор модели.** Скрипт выбора ОС, на которой сейчас работаете |
| `./setup.sh` | **Подготовка к сборке** Подготовка общей среды разработки (папки и т.д) |
| `./build_MIPS.sh` | **Подготовка к сборке** Скачивает нужные для работы сервера файлы, собирает библиотеку под MIPS |
| `./build_UBUNTU.sh` | **Подготовка к сборке** Скачивает нужные для работы сервера файлы, собирает библиотеку под UBUNTU |
| `./build_WIN.sh` | **Подготовка к сборке** Скачивает нужные для работы сервера файлы, собирает библиотеку под WIN |
| `./choice.sh` | **Подготовка к сборке** Скрипт выбора ОС, которая требуется |

---

## Быстрый старт

``` bash
chmod +x install.sh
./install.sh
```

---

### Готовый бинарный файл будет лежать в build:

    build/mms_server


